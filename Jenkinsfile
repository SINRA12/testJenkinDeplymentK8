@Library('jenkins-shared-library') _  // This must match the name you configured in Jenkins

deploySpringBootApp(
    jarName: 'jenkin-0.0.1-SNAPSHOT.jar'  // Change to match your JAR name in the /target directory
)
